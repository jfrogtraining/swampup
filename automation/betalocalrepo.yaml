localRepositories:
  gradle-prod-local:
      type: gradle
      description: "production deployment"
      xray:
        enabled: true
  gradle-release-local:
      type: gradle
      description: "release build"
      xray:
        enabled: true
  tomcat-local:
      type: generic
      description: "approved copy"
      repoLayout: simple-default
      xray:
        enabled: true
  docker-local:
      type: docker
      repoLayout: simple-default
      description: "docker development repo"
      xray:
        enabled: true
  docker-prod-local:
      type: docker
      repoLayout: simple-default
      description: "production deployment"
      xray:
        enabled: true
  docker-stage-local:
      type: docker
      repoLayout: simple-default
      description: "staging deployment"
      xray:
        enabled: true
  libs-release-local:
      type: maven
      repoLayout: maven-2-default
      description: "maven release libraries"
      xray:
        enabled: true
  helm-local:
      type: helm
      repoLayout: simple-default
      description: "helm local "
      xray:
        enabled: true
  generic-local:
      type: generic
      repoLayout: simple-default
      description: "generic local"
      xray:
        enabled: true
  maven-data-local:
      type: maven
      repoLayout: maven-2-default
      description: "maven repo for data generation"
      xray:
        enabled: true
  maven-release-local:
      type: maven
      repoLayout: maven-2-default
      description: "maven repo for data generation"
      xray:
        enabled: true
  maven-snapshot-local:
      type: maven
      repoLayout: maven-2-default
      description: "maven repo for data generation"
      xray:
        enabled: true
  conan-local:
      type: conan
      repoLayout: conan-default
      description: "conan development local"
      xray:
        enabled: true
  debian-dev-local:
      type: debian
      repoLayout: simple-default
      description: "debian development local"
      xray:
        enabled: true
  rpm-dev-local:
      type: yum
      repoLayout: simple-default
      description: "rpm development local"
      xray:
        enabled: true
